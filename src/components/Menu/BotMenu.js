import React from 'react';

function BotMenu(text, buttonLink) {
    return <div className="botMenu">
        <div className="botMenu__left">
            <h1>{text}</h1>
        </div>
    </div>
}

export default BotMenu;
